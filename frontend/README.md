# Frontend (Next.js)

## Setup

1. Install deps:

```bash
npm install
```

2. Configure API base URL:

Create `env.local` (or export env var) with:

```
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

3. Run:

```bash
npm run dev
```

